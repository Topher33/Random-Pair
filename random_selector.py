import os
import subprocess
import time
import datetime
from apscheduler.scheduler import Scheduler
import Random_Pair.integrations as integrations

n_assigned_list = []
assigned_list = []

while len(n_assigned_list) > 0:
    random_pair

def random_pair():
    date = time.strftime("%m/%d/%Y")
    pair = []
    while len(pair) < 2:
        p = n_assigned_list.pop()
        pair.append(p)
    else
        assigned_list.append(data:[p])
        print pair

def unassigned_list():
    # Returns list of remaining teamates list not yet assigned pair
    print n_assigned_list

def assigned_list():
    # Returns list of teamates list already assigned pair
    print assigned_list

def last_assigned(teamate):
    

class Teammate(name):

    def __init__(self, email, last_paired):
            """Return a new teammate object."""
            self.email = email
            self.last_paired = last_assigned(self)
